{
  "name": "anmerko",
  "version": "0.5.3",
  "private": true,
  "type": "module",
  "description": "Turn website comments into an AI-ready brief.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "chrome:install": "node scripts/install-chrome.mjs",
    "test:installer": "node --test tests/tooling/*.test.mjs",
    "lint": "oxlint --deny-warnings .",
    "typecheck": "tsc --noEmit",
    "test": "playwright test -c tests/playwright.config.ts",
    "check": "npm run lint && npm run typecheck && npm run build && npm run test:installer && npm test",
    "demo": "node scripts/demo.mjs",
    "package": "npm run build && node scripts/package.mjs",
    "site:build": "astro build --root site && node scripts/build-site.mjs && npm run store:build",
    "site:dev": "node scripts/serve-site.mjs",
    "site:check": "node --test tests/store-site/*.test.mjs",
    "site:test": "playwright test -c tests/playwright.site.config.ts",
    "site:deploy": "wrangler deploy --config site/wrangler.jsonc",
    "store:build": "node scripts/build-store-site.mjs",
    "store:deploy": "npm run store:build && wrangler deploy --config site/support/wrangler.jsonc",
    "build:firefox": "node scripts/build.mjs --firefox",
    "lint:firefox": "node scripts/web-ext.mjs lint --source-dir dist-firefox --self-hosted --warnings-as-errors",
    "package:firefox": "npm run build:firefox && npm run lint:firefox && node scripts/package.mjs --firefox && node scripts/package-firefox-source.mjs",
    "firefox:run": "node scripts/web-ext.mjs run --source-dir dist-firefox",
    "firefox:android": "node scripts/web-ext.mjs run --source-dir dist-firefox --target firefox-android",
    "firefox:sign": "node scripts/sign-firefox.mjs",
    "test:firefox": "node --test tests/firefox/extension.test.mjs",
    "test:desktop": "node scripts/test-desktop.mjs",
    "test:release": "node --test tests/release/*.test.mjs",
    "release": "gh workflow run release.yml"
  },
  "devDependencies": {
    "@astrojs/markdown-remark": "7.3.0",
    "@astrojs/starlight": "0.42.0",
    "@playwright/test": "1.63.0",
    "@types/chrome": "0.2.9",
    "@types/node": "22.20.2",
    "astro": "7.3.2",
    "esbuild": "0.28.2",
    "oxlint": "1.82.0",
    "selenium-webdriver": "4.49.0",
    "typescript": "7.0.2",
    "web-ext": "10.6.0",
    "wrangler": "4.131.1"
  },
  "overrides": {
    "addons-linter": {
      "image-size": "2.0.4"
    }
  }
}
