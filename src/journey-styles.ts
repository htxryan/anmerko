import journeyStyles from './journey.css';
import imageReviewStyles from './journey-image-review.css';

// Every surface that mounts the journey review also hosts its mask dialog.
export const journeySurfaceStyles = journeyStyles + imageReviewStyles;
